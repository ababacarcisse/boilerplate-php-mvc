<?php

namespace App\Validations;

use Core\Validator;

/**
 * Généré automatiquement par Coud le " . date('Y-m-d H:i') . "
 * Validation: BudgetValidator
 */
class BudgetValidator extends Validator
{
    public function validate(array $data) {
        // Exemple :
        // if (empty($data['champ'])) {
        //     return false;
        // }
        // return true;
    }
}