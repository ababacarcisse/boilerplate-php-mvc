# Pour XAMPP, le fichier se trouve généralement dans:
# /opt/lampp/apache2/conf/httpd.conf

# Assurez-vous que cette ligne est décommentée (pas de # devant):
LoadModule rewrite_module modules/mod_rewrite.so 